{
  "name": "pi-solo",
  "version": "0.1.0",
  "type": "module",
  "description": "Pi extensions for Soloterm — agent orchestration, dispatch, and fork workflows",
  "keywords": [
    "pi-package",
    "pi-extension",
    "soloterm",
    "agent-orchestration"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fabkho/pi-solo.git"
  },
  "files": [
    "extensions",
    "src",
    "README.md"
  ],
  "pi": {
    "extensions": [
      "./extensions/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
